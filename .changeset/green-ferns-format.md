---
'@waldjs/compiler': minor
'@waldjs/cli': minor
---

Add a built-in, idempotent formatter for `.wald` files. Use the exported `format()` compiler API or run `wald format`; `wald format --check` verifies formatting without writing and returns a failing exit status for CI.
