---
"@waldjs/cli": minor
---

Add `oesterAdapter()`, which builds to `.oester/output/` with the `manifest.json` the Oester hosting platform reads, so a WaldJS site deploys to Oester without further setup, also under a `base`.
